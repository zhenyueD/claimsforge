# Gemini API key — get one free at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Gemini model overrides (optional)
GEMINI_TEXT_MODEL=gemini-2.5-flash
GEMINI_VISION_MODEL=gemini-2.5-flash

# Server
PORT=8000

# Legacy OpenAI-compatible fallback (we don't use it, but old conversation_engine respects it)
# LLM_API_KEY=
# LLM_BASE_URL=
# LLM_MODEL=
