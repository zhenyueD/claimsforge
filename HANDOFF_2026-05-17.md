# Handoff · 2026-05-17 morning

> Yesterday's full log: [obsidian_2026_05_16_claimsforge_v5_今日成果](../../.claude/projects/-Users-duan/memory/obsidian_2026_05_16_claimsforge_v5_今日成果.md)
>
> **Hackathon deadline: 2026-05-19 22:00 CST** (~2.5 days from this handoff).
> Prod: http://45.32.154.255 · Repo: github.com/zhenyueD/claimsforge · Latest commit: `0c0b710`

---

## State right now

**Code**: feature-complete for v5. Don't add new functionality.
**Prod**: systemctl active, all endpoints responding, fraud gate verified end-to-end.
**E2E**: 100% (13/13) baseline. 4 new adversarial cases added (EN-11/12/13, ZH-04). The 17/17 number is pending — Gemini API hit transient SSL errors locally last night so the full re-run didn't complete. **Re-run as the very first action this morning** to confirm.

**What's deployed but unverified at 17/17**: fraud gate · amount sandbox · data flywheel · synthesis lock · session_claim_counts persistence · adversarial e2e suite · rate limit (60/min/IP via X-Forwarded-For).

---

## Day plan (priority order)

### 1. Re-run E2E to confirm 17/17  (~5 min)

```bash
cd ~/code/claimsforge && source .venv/bin/activate
unset HTTPS_PROXY http_proxy https_proxy HTTP_PROXY   # last night's proxy env leaked into google-genai
python3 eval/run_e2e_eval.py --concurrency 2 2>&1 | tail -25
```

Expect 17/17 (or 16/17 if one Gemini call still flakes — re-run that case alone).
If still SSL-failing, check `ping generativelanguage.googleapis.com` and try without proxy.
**Write the actual number into README + slides + submission form once green.**

### 2. Update demo-video script for v5  (~30 min)

`docs/demo-video-script.md` is from v3. It misses:
- 7-agent pipeline (was 4)
- SupervisorAgent + the demo'able fraud-replay attack
- Resolution Node 3-button UX
- Data flywheel narrative

**New 2:30 outline** (user records):
- 0:00-0:15 Hook: "$743B in e-commerce returns; most low-value claims still take 3 days of human back-and-forth"
- 0:15-0:30 Pain: screenshot a 3-day email thread
- 0:30-0:50 Walkthrough: "Try Demo: Broken Mug" → watch the 7 agents stream live → 12 sec resolution with $24 refund + bilingual reply
- 0:50-1:30 **The "wow" moment**: re-submit the same photo on a new session → SupervisorAgent's pHash gate fires → "FRAUD_REPLAY: cross-session collision, Hamming=0" → routed to P0 handoff queue. Switch tab to /admin and show the structured briefing the human agent receives (no need to re-read the chat).
- 1:30-2:00 Architecture: pipeline diagram → highlight Supervisor as the "pure Python safety gate beside the LLM judgement" → mention data flywheel (gold cases → Vertex fine-tune NDJSON)
- 2:00-2:15 Business: 90% auto-resolution + lower CSAT + human only sees the edge cases (admin handoff screenshot)
- 2:15-2:30 CTA: claimsforge.example.com · Gemini 2.5 Flash + Vision + Function Calling · MIT

Help the user by drafting this verbatim in `docs/demo-video-script.md`.

### 3. Prep lablab.ai submission fields  (~45 min)

`docs/submission-form.md` exists but is v3-stale. Need fresh:
- **Short description (50 chars)**: "Multi-agent Gemini-powered claims resolution + Sierra-style safety net"
- **Long description (3 paras)**: pull from new README intro
- **Tech tags**: Gemini 2.5 Flash, Gemini Vision, Multi-Agent, Function Calling, Structured Output, Perceptual Hashing, RAG
- **Tracks**: Agentic Workflows + Enterprise Utility + Multimodal Intelligence + Collaborative Systems
- **Cover image**: regen architecture.png with Supervisor + Handoff queue shown
- **Demo URL**: http://45.32.154.255 (mention "no auth for evaluator convenience; rate-limited 30/min/IP")
- **Video URL**: TBD after user records
- **Slides URL**: TBD (slides are 14pp v4, may need 1-2 new pages for Supervisor + Fraud gate + Handoff queue)

### 4. README refresh  (~20 min)

Current README is v4. Add:
- Update agent count 6→7
- Section "Zero-Trust Visual Fraud Gate" (pHash design + Supervisor Rule 0)
- Section "Deterministic Amount Sandbox" (why LLM doesn't compute money)
- Section "Data Flywheel" (gold/red_flag schema + /api/admin/finetune-dataset)
- Update KB count 911→1458, methodologies 47
- Add "Known Limitations" honesty section (single-process session store, 3 demo images, rate limit 30/min)
- Latency note: "p50 9-10s; the bottleneck is per-Gemini-call latency (~1.5-3s × 5 sequential calls). Sprint 1: confidence-gated verifier skip aims to bring p50 under 6s."

---

## Don't do

- **Don't add new features.** Anything that takes >1h goes to Sprint 1. Trust the v5 surface.
- **Don't refactor** api/main.py into routers, **don't delete** legacy `conversation_engine`/`vector_store` (they're still wired to `/api/chat` and `/api/vector/build`). Risk > reward in 2.5 days.
- **Don't try to fine-tune** anything. Vertex AI billing isn't set up, and we have 6 gold cases — not 2000. The export endpoint is the artifact; the fine-tune itself is Sprint 1.
- **Don't push to GitHub without 7890 proxy.** Direct HTTPS to github.com keeps failing on this machine.
  ```bash
  https_proxy=http://127.0.0.1:7890 http_proxy=http://127.0.0.1:7890 git push origin main
  ```
- **Don't commit private data.** `uploads_inbox/`, `data/handoff_queue.jsonl`, `data/session_claim_counts.json`, `data/image_fingerprints.jsonl` are all already gitignored — keep it that way.

---

## Quick reference

### Prod redeploy (after any code change)

```bash
scp -o ConnectTimeout=15 agents/*.py root@45.32.154.255:/opt/claimsforge/agents/
scp -o ConnectTimeout=15 api/main.py root@45.32.154.255:/opt/claimsforge/api/main.py
scp -o ConnectTimeout=15 web/*.html root@45.32.154.255:/opt/claimsforge/web/
ssh root@45.32.154.255 'systemctl restart claimsforge && sleep 3 && systemctl is-active claimsforge'
```

### Prod fraud-gate smoke (for video rehearsal)

```bash
# Upload image
IID=$(curl -s -X POST http://45.32.154.255/api/upload-image \
  -F "file=@data/demo_images/mug_crack.jpg" | python3 -c "import json,sys; print(json.load(sys.stdin)['image_id'])")

# 1st claim (will approve)
curl -s -X POST http://45.32.154.255/api/claim -H 'Content-Type: application/json' \
  -d "{\"session_id\":\"demo-A\",\"message\":\"My mug arrived cracked\",\"image_id\":\"$IID\"}"

# 2nd claim same image new session (will FORCE_ESCALATE on pHash)
curl -s -X POST http://45.32.154.255/api/claim -H 'Content-Type: application/json' \
  -d "{\"session_id\":\"demo-B\",\"message\":\"My mug arrived cracked\",\"image_id\":\"$IID\"}"
```

### Git commit footer (uses zhenyueD's noreply email)

```bash
GIT_COMMITTER_NAME=zhenyueD GIT_COMMITTER_EMAIL=29148014+zhenyueD@users.noreply.github.com \
  git -c user.email=29148014+zhenyueD@users.noreply.github.com commit \
  --author="zhenyueD <29148014+zhenyueD@users.noreply.github.com>" -m "..."
```

---

## Open user-blocking items

| # | Item | Status | Why it matters |
|---|---|---|---|
| #19 | Record demo video | in_progress | Required submission |
| #21 | Fill lablab.ai form | in_progress | Required submission |
| #22 | Enable GCP billing for Gemini key | pending | Optional — only needed for Vertex fine-tune (Sprint 1) |
| #40 | WeChat hermes webhook | pending | Optional — can be dropped from submission narrative |

---

## Emotional / context note

User explicitly said yesterday: "**世界一流的顶级带智慧的，识别不出来是 AI 的产品。同时代码简洁 robust，但是又能调动大模型的智慧**" and "**不允许轻易就满足，把自己的标准放高一点**". Maintain that bar today: every change should be either *demo-visible* or *defensible to evaluators*. No invisible refactors.
